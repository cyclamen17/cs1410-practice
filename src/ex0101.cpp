#include <iostream>

using namespace std;

int main () {
    int x, y;
    cout << "Enter x: ";
    cin >> x;

    cout << "Enter y: ";
    cin >> y;

    cout << "x + y is equal to: " << (x + y) << endl;
    cout << "x - y is equal to: " << (x - y) << endl;
    cout << "x * y is equal to: " << (x * y) << endl;
    cout << " x / y is equal to: " << (x / y) << endl;
    
        return 0;
}